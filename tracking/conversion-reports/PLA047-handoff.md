# PLA047 변환 인수인계 (미변환 사유 + 수동 처리 가이드)

- 생성 시각: 2026-09-05T08:13:40
- TO-BE 패키지: `com.skhynix.gscm.r.pm.pla`
- 생성된 파일: 5개 (Pla047Api.java, Pla047Dto.java, Pla047Mapper.xml, Pla047Service.java, Pla047Store.java)
- 사람이 반드시 처리해야 할 항목: **0건**, 확인 권장: 19건

> 이 문서는 파이프라인이 이미 만든 결과(계획서·생성 이슈·정적 검증·품질 스캔)를 사람이 읽을 순서로 재구성한 것입니다. 자동 변환 결과는 **사람 리뷰 없이 커밋/배포하지 않습니다.**

## ⚠️ AS-IS 원본 자체가 컴파일되지 않는 상태입니다

중괄호 불일치: {'F.java': 11}. 원본 결함은 고치지 않고 그대로 옮기는 게 이 프로젝트 원칙이라, 변환 결과에도 같은 결함이 남아 있을 수 있습니다. CLAUDE.md는 이런 화면을 **Reimagine 트랙** (업무 규칙만 추출해 재설계) 후보로 봅니다 - 트랙 결정은 사람이 합니다.

## 🔴 반드시 사람이 처리해야 할 것 (BLOCKER)

없습니다.

## 🟡 확인이 필요한 것 (WARNING)

- **REMAPRESULTS_DROPPED** — remapresults 속성 발견 - MyBatis에 대응 기능 없음, 제거 예정. 결과 컬럼명 중복 여부 확인 필요
  - 발견: Mapper 변환(converters)
  - 조치: remapResults 속성은 MyBatis에 대응이 없어 제거했습니다. 동작 차이가 없는지 확인하세요.
- **DTO_FIELD_EXTRACT_INCOMPLETE** (메서드 `pPLA04702`) — pPLA04702: fPLA047QrySelectRev에서 개별 getField 호출을 찾지 못했습니다 (getFieldMap()으로 통째로 넘기는 구조일 수 있음) - 요청 필드를 수동으로 확인하세요.
  - 발견: DTO 생성(skeleton_gen)
  - 조치: 요청/응답 필드를 자동으로 못 뽑아 DTO에 TODO가 남아 있습니다(F가 getFieldMap()으로 통째로 넘기는 구조 등). 원본에서 실제 사용 필드를 확인해 채우세요 - 추측으로 채우지 마세요.
- **DTO_FIELD_EXTRACT_INCOMPLETE** (메서드 `pPLA04703`) — pPLA04703: fPLA047QrySelectRevPeriod에서 개별 getField 호출을 찾지 못했습니다 (getFieldMap()으로 통째로 넘기는 구조일 수 있음) - 요청 필드를 수동으로 확인하세요.
  - 발견: DTO 생성(skeleton_gen)
  - 조치: 요청/응답 필드를 자동으로 못 뽑아 DTO에 TODO가 남아 있습니다(F가 getFieldMap()으로 통째로 넘기는 구조 등). 원본에서 실제 사용 필드를 확인해 채우세요 - 추측으로 채우지 마세요.
- **SQL_INJECTION_RISK** (5046행) — ${TECH_GRP_ID}가 1곳(5046행)에서 발견됨: 조건절(WHERE/AND/OR/LIKE)에서 값이 SQL 텍스트에 직접 섞입니다 - 외부 입력 경로를 타면 실제 인젝션으로 이어질 수 있습니다. 가능하면 #{TECH_GRP_ID}(파라미터 바인딩)로 바꿀 수 있는지 우선 검토하세요.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5046행) — ${YEAR}가 3곳(5046, 5047, 5053행)에서 발견됨: 조건절(WHERE/AND/OR/LIKE)에서 값이 SQL 텍스트에 직접 섞입니다 - 외부 입력 경로를 타면 실제 인젝션으로 이어질 수 있습니다. 가능하면 #{YEAR}(파라미터 바인딩)로 바꿀 수 있는지 우선 검토하세요.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5046행) — ${NETDIE_WHERE}가 3곳(5046, 5047, 5053행)에서 발견됨: 조건절(WHERE/AND/OR/LIKE)에서 값이 SQL 텍스트에 직접 섞입니다 - 외부 입력 경로를 타면 실제 인젝션으로 이어질 수 있습니다. 가능하면 #{NETDIE_WHERE}(파라미터 바인딩)로 바꿀 수 있는지 우선 검토하세요.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 242행) — 242행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 426행) — 426행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 427행) — 427행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 429행) — 429행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 430행) — 430행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 433행) — 433행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 434행) — 434행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 440행) — 440행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 441행) — 441행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 443행) — 443행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 444행) — 444행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 447행) — 447행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.
- **DYNAMIC_SQL_STRING_CONCAT** (메서드 `fPLA047QrySelectMainList`, 448행) — 448행: 문자열 연결로 SQL 조각을 조립하고 있다 - 값이 외부 입력에서 오는 경로가 있다면 SQL 인젝션 위험, 아니어도 유지보수 시 실수 유발 가능. 바인드 변수로 대체할 수 있는지 검토할 것.
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 문자열 연결로 SQL을 조립합니다. 바인드 변수로 대체 가능한지 검토하세요.

<details><summary>참고 항목 (INFO)</summary>

- **CDATA_SIMPLIFIED** — 특수문자(&/</>)가 없어 불필요했던 CDATA 블록 488개를 일반 텍스트로 정리했습니다. 특수문자가 있어 실제로 필요한 CDATA 18개는 MyBatis가 CDATA를 그대로 지원하므로 그대로 유지했습니다(엔티티 이스케이프로 억지 변환하지 않음).
  - 발견: Mapper 변환(converters)
  - 조치: CDATA 처리를 단순화했습니다. SQL 의미가 바뀌지 않았는지 확인하세요.
- **FETCH_SIZE_DROPPED** — fetchSize 속성은 MyBatis 변환 시 제거했습니다 - 필요하면 <select>에 수동으로 다시 넣으세요.
  - 발견: Mapper 변환(converters)
  - 조치: fetchSize 속성을 제거했습니다. 성능이 중요하면 MyBatis 설정으로 다시 지정하세요.
- **SQL_INJECTION_RISK** (8행) — ${DIM}가 22곳(8, 138, 372, 640, 660 외 17건행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (432행) — ${DIMORG}가 18곳(432, 487, 617, 971, 1026 외 13건행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4194행) — ${DIM_OUTER}가 4곳(4194, 5386, 5481, 5578행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4217행) — ${CUM2_YLD_MASTER_DATA}가 2곳(4217, 5500행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4220행) — ${GOOD_DIE_MASTER_DATA}가 2곳(4220, 5502행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4223행) — ${WF_CONV_QTY_SALE_PLN}가 2곳(4223, 5504행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4226행) — ${SOM_EQ_QTY_SALE_PLN}가 3곳(4226, 4437, 5505행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4229행) — ${SOM_DIE_QTY_SALE_PLN}가 2곳(4229, 5506행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4232행) — ${ORG_QTY_SALE_PLN}가 2곳(4232, 5507행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4235행) — ${SALE_AMT_SALE_PLN}가 2곳(4235, 5508행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4238행) — ${ASP_SALE_PLN}가 3곳(4238, 4438, 5509행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4241행) — ${SALE_GRAV_SALE_PLN}가 3곳(4241, 4442, 5510행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4244행) — ${QTY_GRAV_SALE_PLN}가 3곳(4244, 4443, 5511행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4247행) — ${SALES_PROF_GRAV}가 3곳(4247, 4444, 5512행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4251행) — ${SALE_WF_SLS_PROF}가 2곳(4251, 5513행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4254행) — ${CNTRB_PROF_WF_SLS_PROF_ST_O}가 2곳(4254, 5098행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4257행) — ${CASH_PROF_WF_SLS_PROF_ST_O}가 2곳(4257, 5101행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4260행) — ${SALE_PROF_WF_SLS_PROF_ST_O}가 2곳(4260, 5104행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4263행) — ${SALES_PROF_WF_SLS_PROF_ST_O}가 2곳(4263, 5107행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4267행) — ${COO_COST_WF_SLS_COST_ST_O}가 4곳(4267, 4376, 5111, 5220행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4270행) — ${COO_PROF_RATE_WF_COL}가 2곳(4270, 5114행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4274행) — ${SALE_EQ_PROF}가 2곳(4274, 5518행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4277행) — ${CNTRB_PROF_EQ_PROF_ST_O}가 2곳(4277, 5121행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4280행) — ${CASH_PROF_EQ_PROF_ST_O}가 2곳(4280, 5124행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4283행) — ${SALE_PROF_EQ_PROF_ST_O}가 2곳(4283, 5127행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4286행) — ${SALES_PROF_EQ_PROF_ST_O}가 2곳(4286, 5130행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4290행) — ${COO_COST_EQ_COST_ST_O}가 4곳(4290, 4390, 5134, 5234행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4293행) — ${COO_PROF_RATE_EQ_COL}가 2곳(4293, 5137행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4297행) — ${SALE_DIE_PROF}가 2곳(4297, 5523행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4300행) — ${CNTRB_PROF_DIE_PROF_ST_O}가 2곳(4300, 5144행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4303행) — ${CASH_PROF_DIE_PROF_ST_O}가 2곳(4303, 5147행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4306행) — ${SALE_PROF_DIE_PROF_ST_O}가 2곳(4306, 5150행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4309행) — ${SALES_PROF_DIE_PROF_ST_O}가 2곳(4309, 5153행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4313행) — ${COO_COST_DIE_COST_ST_O}가 4곳(4313, 4404, 5157, 5248행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4316행) — ${COO_PROF_RATE_DIE_COL}가 2곳(4316, 5160행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4320행) — ${SALE_PROD_PROF}가 3곳(4320, 5164, 5528행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4323행) — ${CNTRB_PROF_PROD_PROF_ST_O}가 2곳(4323, 5167행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4326행) — ${CASH_PROF_PROD_PROF_ST_O}가 2곳(4326, 5170행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4329행) — ${SALE_PROF_PROD_PROF_ST_O}가 2곳(4329, 5173행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4332행) — ${SALES_PROF_PROD_PROF_ST_O}가 2곳(4332, 5176행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4336행) — ${COO_COST_PROD_COST_ST_O}가 4곳(4336, 4418, 5180, 5262행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4339행) — ${COO_PROF_RATE_PROD_COL}가 2곳(4339, 5183행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4343행) — ${SALE_YR_PROF}가 2곳(4343, 5533행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4346행) — ${CNTRB_PROF_YR_PROF}가 2곳(4346, 5534행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4349행) — ${CASH_PROF_YR_PROF}가 2곳(4349, 5535행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4352행) — ${SALE_PROF_YR_PROF}가 2곳(4352, 5536행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4355행) — ${SALES_PROF_YR_PROF}가 2곳(4355, 5537행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4359행) — ${COO_COST_YR_COST}가 3곳(4359, 4432, 5557행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4362행) — ${COO_PROF_RATE_YR}가 2곳(4362, 5565행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4366행) — ${VAR_COST_WF_SLS_COST_ST_O}가 2곳(4366, 5210행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4369행) — ${CASH_COST_WF_SLS_COST_ST_O}가 2곳(4369, 5213행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4372행) — ${COGS_COST_WF_SLS_COST_ST_O}가 2곳(4372, 5216행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4380행) — ${VAR_COST_EQ_COST_ST_O}가 2곳(4380, 5224행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4383행) — ${CASH_COST_EQ_COST_ST_O}가 2곳(4383, 5227행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4386행) — ${COGS_COST_EQ_COST_ST_O}가 2곳(4386, 5230행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4394행) — ${VAR_COST_DIE_COST_ST_O}가 2곳(4394, 5238행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4397행) — ${CASH_COST_DIE_COST_ST_O}가 2곳(4397, 5241행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4400행) — ${COGS_COST_DIE_COST_ST_O}가 2곳(4400, 5244행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4408행) — ${VAR_COST_PROD_COST_ST_O}가 2곳(4408, 5252행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4411행) — ${CASH_COST_PROD_COST_ST_O}가 2곳(4411, 5255행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4414행) — ${COGS_COST_PROD_COST_ST_O}가 2곳(4414, 5258행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4422행) — ${VAR_COST_YR_COST}가 2곳(4422, 5554행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4425행) — ${CASH_COST_YR_COST}가 2곳(4425, 5555행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4428행) — ${COGS_COST_YR_COST}가 2곳(4428, 5556행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4538행) — ${DIM_OUTER_SEQ}가 2곳(4538, 5380행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4546행) — ${PIVOT_STR}가 2곳(4546, 5572행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4553행) — ${ORDER_BY}가 2곳(4553, 5601행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4792행) — ${GROUPING_NETDIE1}가 3곳(4792, 4841, 4906행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4819행) — ${GROUPING_NETDIE2}가 2곳(4819, 4928행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4842행) — ${GROUPING_NETDIE4}가 1곳(4842행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4865행) — ${GROUPING_NETDIE3}가 4곳(4865, 4886, 4951, 4976행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4952행) — ${GROUPING_NETDIE5}가 1곳(4952행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (4999행) — ${GROUPING_WHERE}가 1곳(4999행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5022행) — ${DIM_OUTER_SUB_TOTAL}가 1곳(5022행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5061행) — ${CUM2_COL}가 1곳(5061행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5064행) — ${GOOD_DIE_COL}가 1곳(5064행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5067행) — ${WF_CONV_QTY_SALE_PLN_ST_O}가 1곳(5067행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5070행) — ${SOM_EQ_QTY_SALE_PLN_ST_O}가 2곳(5070, 5281행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5073행) — ${SOM_DIE_QTY_SALE_PLN_ST_O}가 1곳(5073행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5076행) — ${ORG_QTY_SALE_PLN_ST_O}가 1곳(5076행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5079행) — ${SALE_AMT_SALE_PLN_ST_O}가 1곳(5079행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5082행) — ${ASP_SALE_PLN_ST_O}가 2곳(5082, 5282행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5085행) — ${SALE_GRAV_SALE_PLN_ST_O}가 2곳(5085, 5286행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5088행) — ${QTY_GRAV_SALE_PLN_ST_O}가 2곳(5088, 5287행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5091행) — ${SALES_PROF_GRAV_ST_O}가 2곳(5091, 5288행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5095행) — ${SALE_WF_SLS_PROF_ST_O}가 1곳(5095행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5118행) — ${SALE_EQ_PROF_ST_O}가 1곳(5118행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5141행) — ${SALE_DIE_PROF_ST_O}가 1곳(5141행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5187행) — ${SALE_YR_PROF_ST_O}가 1곳(5187행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5190행) — ${CNTRB_PROF_YR_PROF_ST_O}가 1곳(5190행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5193행) — ${CASH_PROF_YR_PROF_ST_O}가 1곳(5193행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5196행) — ${SALE_PROF_YR_PROFV}가 1곳(5196행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5199행) — ${SALES_PROF_YR_PROF_ST_O}가 1곳(5199행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5203행) — ${COO_COST_YR_COST_ST_O}가 2곳(5203, 5276행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5206행) — ${COO_PROF_RATE_YR_ST_O}가 1곳(5206행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5266행) — ${VAR_COST_YR_COST_ST_O}가 1곳(5266행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5269행) — ${CASH_COST_YR_COST_ST_O}가 1곳(5269행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5272행) — ${COGS_COST_YR_COST_ST_O}가 1곳(5272행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5387행) — ${DIM_GROUP}가 1곳(5387행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5409행) — ${CUM2_YLD_MASTER_DATA_ST}가 1곳(5409행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5410행) — ${ODD_CUM2_YLD_MASTER_DATA_ST}가 1곳(5410행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5411행) — ${GOOD_DIE_MASTER_DATA_ST}가 1곳(5411행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5412행) — ${ODD_GOOD_DIE_MASTER_DATA_ST}가 1곳(5412행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5413행) — ${WF_CONV_QTY_SALE_PLN_ST}가 1곳(5413행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5414행) — ${SOM_EQ_QTY_SALE_PLN_ST}가 1곳(5414행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5415행) — ${SOM_DIE_QTY_SALE_PLN_ST}가 1곳(5415행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5416행) — ${ORG_QTY_SALE_PLN_ST}가 1곳(5416행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5417행) — ${SALE_AMT_SALE_PLN_ST}가 1곳(5417행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5418행) — ${ASP_SALE_PLN_ST}가 1곳(5418행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5419행) — ${SALE_GRAV_SALE_PLN_ST}가 1곳(5419행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5420행) — ${QTY_GRAV_SALE_PLN_ST}가 1곳(5420행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5421행) — ${SALES_PROF_GRAV_ST}가 1곳(5421행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5422행) — ${SALE_WF_SLS_PROF_ST}가 1곳(5422행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5423행) — ${CNTRB_PROF_WF_SLS_PROF_ST}가 1곳(5423행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5424행) — ${CASH_PROF_WF_SLS_PROF_ST}가 1곳(5424행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5425행) — ${SALE_PROF_WF_SLS_PROF_ST}가 1곳(5425행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5426행) — ${SALES_PROF_WF_SLS_PROF_ST}가 1곳(5426행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5427행) — ${SALE_EQ_PROF_ST}가 1곳(5427행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5428행) — ${CNTRB_PROF_EQ_PROF_ST}가 1곳(5428행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5429행) — ${CASH_PROF_EQ_PROF_ST}가 1곳(5429행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5430행) — ${SALE_PROF_EQ_PROF_ST}가 1곳(5430행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5431행) — ${SALES_PROF_EQ_PROF_ST}가 1곳(5431행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5432행) — ${SALE_DIE_PROF_ST}가 1곳(5432행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5433행) — ${CNTRB_PROF_DIE_PROF_ST}가 1곳(5433행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5434행) — ${CASH_PROF_DIE_PROF_ST}가 1곳(5434행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5435행) — ${SALE_PROF_DIE_PROF_ST}가 1곳(5435행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5436행) — ${SALES_PROF_DIE_PROF_ST}가 1곳(5436행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5437행) — ${SALE_PROD_PROF_ST}가 1곳(5437행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5438행) — ${CNTRB_PROF_PROD_PROF_ST}가 1곳(5438행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5439행) — ${CASH_PROF_PROD_PROF_ST}가 1곳(5439행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5440행) — ${SALE_PROF_PROD_PROF_ST}가 1곳(5440행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5441행) — ${SALES_PROF_PROD_PROF_ST}가 1곳(5441행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5442행) — ${SALE_YR_PROF_ST}가 1곳(5442행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5443행) — ${CNTRB_PROF_YR_PROF_ST}가 1곳(5443행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5444행) — ${CASH_PROF_YR_PROF_ST}가 1곳(5444행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5445행) — ${SALE_PROF_YR_PROF_ST}가 1곳(5445행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5446행) — ${SALES_PROF_YR_PROF_ST}가 1곳(5446행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5447행) — ${VAR_COST_WF_SLS_COST_ST}가 1곳(5447행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5448행) — ${CASH_COST_WF_SLS_COST_ST}가 1곳(5448행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5449행) — ${COGS_COST_WF_SLS_COST_ST}가 1곳(5449행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5450행) — ${COO_COST_WF_SLS_COST_ST}가 1곳(5450행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5451행) — ${VAR_COST_EQ_COST_ST}가 1곳(5451행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5452행) — ${CASH_COST_EQ_COST_ST}가 1곳(5452행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5453행) — ${COGS_COST_EQ_COST_ST}가 1곳(5453행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5454행) — ${COO_COST_EQ_COST_ST}가 1곳(5454행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5455행) — ${VAR_COST_DIE_COST_ST}가 1곳(5455행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5456행) — ${CASH_COST_DIE_COST_ST}가 1곳(5456행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5457행) — ${COGS_COST_DIE_COST_ST}가 1곳(5457행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5458행) — ${COO_COST_DIE_COST_ST}가 1곳(5458행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5459행) — ${VAR_COST_PROD_COST_ST}가 1곳(5459행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5460행) — ${CASH_COST_PROD_COST_ST}가 1곳(5460행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5461행) — ${COGS_COST_PROD_COST_ST}가 1곳(5461행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5462행) — ${COO_COST_PROD_COST_ST}가 1곳(5462행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5463행) — ${VAR_COST_YR_COST_ST}가 1곳(5463행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5464행) — ${CASH_COST_YR_COST_ST}가 1곳(5464행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5465행) — ${COGS_COST_YR_COST_ST}가 1곳(5465행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5466행) — ${COO_COST_YR_COST_ST}가 1곳(5466행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5470행) — ${COO_PROF_RATE_WF_ST}가 1곳(5470행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5471행) — ${COO_PROF_RATE_EQ_ST}가 1곳(5471행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5472행) — ${COO_PROF_RATE_DIE_ST}가 1곳(5472행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5473행) — ${COO_PROF_RATE_PROD_ST}가 1곳(5473행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5474행) — ${COO_PROF_RATE_YR_ST}가 1곳(5474행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5501행) — ${ODD_CUM2_YLD_MASTER_DATA}가 1곳(5501행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5503행) — ${ODD_GOOD_DIE_MASTER_DATA}가 1곳(5503행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5514행) — ${CNTRB_PROF_WF_SLS_PROF}가 1곳(5514행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5515행) — ${CASH_PROF_WF_SLS_PROF}가 1곳(5515행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5516행) — ${SALE_PROF_WF_SLS_PROF}가 1곳(5516행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5517행) — ${SALES_PROF_WF_SLS_PROF}가 1곳(5517행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5519행) — ${CNTRB_PROF_EQ_PROF}가 1곳(5519행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5520행) — ${CASH_PROF_EQ_PROF}가 1곳(5520행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5521행) — ${SALE_PROF_EQ_PROF}가 1곳(5521행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5522행) — ${SALES_PROF_EQ_PROF}가 1곳(5522행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5524행) — ${CNTRB_PROF_DIE_PROF}가 1곳(5524행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5525행) — ${CASH_PROF_DIE_PROF}가 1곳(5525행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5526행) — ${SALE_PROF_DIE_PROF}가 1곳(5526행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5527행) — ${SALES_PROF_DIE_PROF}가 1곳(5527행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5529행) — ${CNTRB_PROF_PROD_PROF}가 1곳(5529행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5530행) — ${CASH_PROF_PROD_PROF}가 1곳(5530행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5531행) — ${SALE_PROF_PROD_PROF}가 1곳(5531행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5532행) — ${SALES_PROF_PROD_PROF}가 1곳(5532행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5538행) — ${VAR_COST_WF_SLS_COST}가 1곳(5538행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5539행) — ${CASH_COST_WF_SLS_COST}가 1곳(5539행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5540행) — ${COGS_COST_WF_SLS_COST}가 1곳(5540행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5541행) — ${COO_COST_WF_SLS_COST}가 1곳(5541행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5542행) — ${VAR_COST_EQ_COST}가 1곳(5542행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5543행) — ${CASH_COST_EQ_COST}가 1곳(5543행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5544행) — ${COGS_COST_EQ_COST}가 1곳(5544행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5545행) — ${COO_COST_EQ_COST}가 1곳(5545행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5546행) — ${VAR_COST_DIE_COST}가 1곳(5546행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5547행) — ${CASH_COST_DIE_COST}가 1곳(5547행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5548행) — ${COGS_COST_DIE_COST}가 1곳(5548행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5549행) — ${COO_COST_DIE_COST}가 1곳(5549행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5550행) — ${VAR_COST_PROD_COST}가 1곳(5550행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5551행) — ${CASH_COST_PROD_COST}가 1곳(5551행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5552행) — ${COGS_COST_PROD_COST}가 1곳(5552행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5553행) — ${COO_COST_PROD_COST}가 1곳(5553행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5561행) — ${COO_PROF_RATE_WF}가 1곳(5561행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5562행) — ${COO_PROF_RATE_EQ}가 1곳(5562행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5563행) — ${COO_PROF_RATE_DIE}가 1곳(5563행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **SQL_INJECTION_RISK** (5564행) — ${COO_PROF_RATE_PROD}가 1곳(5564행)에서 발견됨: ORDER BY/컬럼·테이블명 동적 치환으로 보여 상대적으로 위험도가 낮게 분류했습니다 - 값의 출처가 코드 상수/고정 목록이 아니라 외부 입력이라면 여전히 검토가 필요합니다.
  - 발견: 품질·취약점 스캔(Pla047Mapper.xml)
  - 조치: `${...}`가 조건절에 쓰였습니다. 값이 외부 입력에서 오면 인젝션 위험이니 가능하면 `#{...}`로 바꿀 수 있는지 검토하세요.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 287행) — 287행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - 제네릭 타입명이 object 소문자로 되어 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 296행) — 296행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - 제네릭 타입명이 object 소문자로 되어 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 305행) — 305행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - 제네릭 타입명이 object 소문자로 되어 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 314행) — 314행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - 제네릭 타입명이 object 소문자로 되어 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 323행) — 323행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - 제네릭 타입명이 object 소문자로 되어 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 332행) — 332행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - 제네릭 타입명이 object 소문자로 되어 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 341행) — 341행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - 제네릭 타입명이 object 소문자로 되어 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 437행) — 437행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - for 블록 종료 구문이 ')'로 되어 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 499행) — 499행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - system 소문자 사용으로 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 505행) — 505행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - 동일 이름 변수 sCooProfRateEq1를 String과 String[]로 중복 선언하여 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 524행) — 524행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - system 소문자 사용으로 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 548행) — 548행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - system 소문자 사용으로 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.
- **ORIGINAL_BUG** (메서드 `fPLA047QrySelectMainList`, 572행) — 572행: 원본 버그(포팅 시 보존, 임의 수정 안 함) - system 소문자 사용으로 컴파일 오류
  - 발견: 품질·취약점 스캔(Pla047Service.java)
  - 조치: 원본에 있던 결함을 고치지 않고 그대로 옮긴 지점입니다(의도된 동작). 업무 규칙을 아는 사람이 고칠지 유지할지 판단해야 합니다.

</details>

## 자동 변환된 산출물

| 파일 | TO-BE 경로 | 변환 방식 |
|---|---|---|
| Pla047Api.java | `gscm/src/main/java/com/skhynix/gscm/r/pm/pla/Controller/Pla047Api.java` | RULE_BASED |
| Pla047Dto.java | `gscm/src/main/java/com/skhynix/gscm/r/pm/pla/dto/Pla047Dto.java` | RULE_BASED |
| Pla047Service.java | `gscm/src/main/java/com/skhynix/gscm/r/pm/pla/service/Pla047Service.java` | RULE_BASED_SKELETON + LLM_PORTING |
| Pla047Store.java | `gscm/src/main/java/com/skhynix/gscm/r/pm/pla/store/Pla047Store.java` | RULE_BASED |
| Pla047Mapper.xml | `gscm/src/main/resources/mapper/r/pm/pla/Pla047Mapper.xml` | RULE_BASED |

## LLM이 포팅한 메서드 (반드시 사람 리뷰)

생성 코드 첫 줄의 `// AI 변경 요약:` 주석에 무엇을 어떻게 옮겼는지 적혀 있습니다.

- `fPLA047QrySelectMainList`
