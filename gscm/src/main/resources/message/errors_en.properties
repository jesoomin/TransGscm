# Message code -> English text. {0},{1}... are MessageFormat placeholders (filled from
# args such as new String[] {"..."} in the AS-IS source).
#
# Only codes actually observed in PLA047 (/legacy/PPLA047.java, /legacy/FPLA047.java) are
# registered here. The real wording is not present in any of these 3 source files (it lives
# only in the NEXCORE common message table) - do not guess it (CLAUDE.md). Fill in the TODOs
# below after checking the NEXCORE common message table.

E0052=TODO: check the actual wording in the NEXCORE common message table ({0})
W0024=TODO: check the actual wording in the NEXCORE common message table ({0})
I0016=TODO: check the actual wording in the NEXCORE common message table ({0})

# New code (not in NEXCORE) - used only by GlobalExceptionHandler to flag a missed
# catch-and-wrap during porting. See docs/09-common-conventions.md.
E9999=An unhandled error occurred: {0}
