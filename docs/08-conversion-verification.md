# 화면·파일별 변환 검증 테이블 설계

## 목적
1,416개 화면(정확히는 그에 대응하는 AS-IS Java/XSQL/.BIZUNIT 파일 묶음)을 순차 변환하면서, **파일 단위로** "원본이 애초에 정상인가 → 어떻게 변환했는가 → 빌드가 되는가 → 기능이 맞는가 → 사람이 승인했는가"를 추적한다. PLA047 작업 중 실제로 겪은 문제(파일 자체가 파싱/컴파일 안 되는 경우, 대응 TO-BE 파일이 없는 경우)를 그대로 열로 반영했다 — 가상의 시나리오가 아니라 이번 세션에서 확인된 상태를 기준으로 설계했다.

행 = **AS-IS 파일 1개** (`.java`, `.xsql`, `.bizunit`). DTO나 메시지 프로퍼티처럼 특정 AS-IS 파일 하나에 대응하지 않고 여러 파일을 취합해서 만드는 TO-BE 산출물은 "AS-IS 계층"을 `(파생)`으로 표시하고 어떤 파일들을 참고했는지 비고에 적는다.

## 컬럼 정의

| 컬럼 | 설명 | 값 예시 |
|---|---|---|
| 화면ID | 화면/트랜잭션 묶음 식별자 | `PLA047` |
| AS-IS 계층 | P/F/D BizUnit(Java), BizUnit XML, XSQL, 또는 `(파생)` | `P(Java)`, `F(BIZUNIT)`, `XSQL`, `(파생)` |
| AS-IS 파일명 | 원본 파일명 | `PPLA047.java` |
| AS-IS 경로 | `/legacy` 기준 상대 경로 | `legacy/PPLA047.java` |
| 대응 TO-BE 파일 | `docs/07-tobe-structure.xlsx` 명명 규칙에 따른 목표 파일명 (1개 AS-IS가 여러 TO-BE에 걸칠 수 있음) | `Pla047Api.java` |
| 대응 TO-BE 경로 | 실제 생성 위치 (없으면 "미생성") | `pilot/PLA047/...` |
| AS-IS 파싱 상태 | 원본 자체가 유효한 포맷인가 (XML 선언, 태그 짝, Java 문법). `.java`는 문법 파싱 가능 여부, XML류는 파서 통과 여부 | `OK` / `실패` / `N/A` |
| AS-IS 컴파일 상태 | `.java`만 해당. javac 기준 컴파일 가능 여부 | `OK` / `실패` / `N/A` |
| 변환 방식 | CLAUDE.md 결정론적/LLM 경계 표에 따른 실제 처리 방식 | `규칙기반` / `LLM포팅` / `규칙기반+LLM포팅` / `수동` |
| 트랙(Refactor/Reimagine) | AWS Transform 이원화 기준(@docs/06-mentor-feedback.md §F). 단순조회/CRUD는 Refactor(1:1 구조보존), 복합화면·특수 로직·원본 자체가 망가진 경우는 Reimagine(업무 규칙만 추출해 재설계) 후보로 표시. **화면 단위 결정이 원칙**이지만 파일별로 다르게 보일 수 있어 우선 파일 단위로 후보만 표시하고, 최종 트랙은 사람이 화면 단위로 확정한다 | `Refactor 후보` / `Reimagine 후보` / `미정(검토 필요)` |
| 변환 상태 | 이 파일 기준 진행 단계 | `미착수` / `진행중` / `완료` / `보류` |
| TO-BE 빌드 검증 | Maven/Gradle 빌드 통과 여부. 변환 상태가 `완료`가 아니면 대부분 `미실행` | `Pass` / `Fail` / `미실행` |
| 차등 테스트 검증 | 레거시 nctRid 응답과 신규 REST 응답 diff 일치 여부 (@docs/02-architecture.md 검증 전략) | `Pass` / `Fail` / `N/A` / `미실행` |
| 사람 수정 라인 비율 | 자동 생성된 코드 중 사람이 리뷰하며 손댄 라인 수 비율. 멘토 코멘트(§H)가 꼽은 "실제 공수 절감률의 가장 직접적인 대리 지표" — 파일럿 이후 KPI 실측에 그대로 쓴다 | `12%` (수정 8줄/전체 65줄) 형태, 코드 생성 전엔 `N/A(미생성)` |
| 리뷰 상태 | 사람 리뷰·승인 여부. `승인` 전까지 커밋 금지(CLAUDE.md 핵심 원칙) | `미검토` / `검토중` / `승인` |
| 블로커/비고 | 막힌 이유나 특이사항을 구체적으로. "확인 필요"처럼 애매하게 쓰지 않는다 | `S003~S006 정의 없음` |
| 최종 갱신일 | YYYY-MM-DD | `2026-08-14` |

## 상태값 규칙
- **변환 상태가 `완료`가 되려면** TO-BE 빌드 검증이 `Pass`여야 한다 (CLAUDE.md: "빌드/린트 통과해야 완료로 인정"). 빌드 전이면 아무리 코드를 다 옮겼어도 `진행중`으로 둔다.
- **AS-IS 파싱/컴파일 상태가 `실패`인 파일은 변환 상태를 `보류`로 두고, 원본 재확보 전까지 `진행중`으로 올리지 않는다.** 깨진 원본 위에서 "그럴듯하게 지어낸" 변환은 CLAUDE.md 핵심 원칙 위반이다.
- **리뷰 상태는 빌드 통과와 별개다.** 빌드가 `Pass`여도 사람이 diff를 보고 `승인`하기 전까지는 커밋하지 않는다.

## 사용 방법
`/tracking/conversion-verification.csv`를 화면 작업할 때마다 갱신한다. 화면 1개 = 보통 7~9행(P/F/D의 .java·.bizunit + XSQL + 파생 DTO/메시지). 파일럿 20~30화면이 끝나면 이 CSV가 "화면 유형별 실측 커버리지"를 그대로 보여주는 산출물이 된다 (@docs/03-kickoff-plan.md Phase 3).

## DB 버전 (2026-08-14 추가, 같은 날 정적 검증 연동 추가)
같은 정보를 사람이 보는 CSV 말고 **DB(로컬 Oracle)에도 자동으로 쌓을 수 있게** `agents/db_schema.sql`(`CONV_FILE`/`CONV_ISSUE` 테이블)과 `agents/db.py`(연결·삽입 함수)를 추가했다. `chatui/app.py`에서 변환을 돌리고 "DB에도 기록" 체크박스(기본 켜짐)를 켜면 그 화면에서 발견된 이슈(태그 불일치, 미지원 태그, remapresults 등)가 `CONV_ISSUE`에 개별 행으로 쌓인다.
- 실제 로컬 Oracle DB(`RPLS_ADM`/`xe`)에 테이블 생성 + insert + select까지 end-to-end로 검증 완료.
- CSV와 DB는 지금은 서로 자동 동기화되지 않는다 — CSV는 사람이 화면 리뷰하며 채우는 요약본, DB는 변환기가 실행될 때마다 쌓이는 원시 로그에 가깝다. 화면 수가 늘면 CSV를 DB에서 뽑아내는 쪽으로 정리하는 게 맞다(아직 안 함).
- v0 한계: 골격 **생성** 단계 이슈(문법/시그니처 관련, `skeleton_gen.py`가 만듦)는 여전히 파일별로 정확히 귀속하지 못하고 P(JAVA) 행에 대표로 붙인다. XSQL/Dto 생성 이슈는 정확히 귀속된다.
- **`chatui/validators.py`(신규, 변환기와 분리된 검증기) 연동**: 골격 생성/포팅 직후 자동으로 정적 검증을 돌려 파일별로 정확히 귀속된 `CONV_FILE.BUILD_CHECK`(PASS/FAIL/NOT_RUN)를 남긴다 — 이건 생성 이슈와 달리 P/F/D/XSQL/Dto 전부 파일 단위로 정확하다. 계층 간 참조(Api→Service→Store→Mapper) 검증은 파일 하나에 속하지 않아 `AS_IS_LAYER=DERIVED`, `AS_IS_FILENAME={화면}-cross-layer-check`인 별도 행으로 기록한다. 실제 Maven/Spring 빌드는 아직 없어(pom.xml 미구축) 진짜 컴파일 검증은 아니고, 중괄호 균형·미완료 포팅 스텁·계층 간 호출 대상 존재·Mapper.xml well-formed 수준의 정적 검사다 - PLA047로 실제 DB에 기록까지 검증 완료.

## PLA047으로 시드한 초기 데이터
이번 세션에서 실제로 확인한 상태를 그대로 첫 9행에 기록했다(추측치 없음):
- P/F/D `.bizunit` 3개 전부 XML 선언 손상 — 파싱 실패
- `PPLA047.java` 컴파일 실패(파라미터 누락), `FPLA047.java` 컴파일 실패(다수 에러), `DPLA047.java`는 문법상 이상 없음(컴파일러로 미검증)
- `DPLA047.xsql`은 S001+queryCommon만 변환 완료, S002~S006은 원본 자체가 없어 보류
- DTO/메시지 프로퍼티는 아직 미착수

상세 근거는 이전 세션의 `/legacy` 검증 작업(본 대화 앞부분) 및 `pilot/PLA047/` 결과물 참고.
