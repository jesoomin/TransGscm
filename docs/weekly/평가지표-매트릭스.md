# 평가 지표 매트릭스

**대상**: G-SCM 차세대 전환 Agent (1단계 — 서버 전환)
**측정 범위**: 파일럿 **5화면**(PLA087·089·093·096·100) + 정답키 세트 30화면
**산출**: `python -m agents.scorecard <폴더> --screens ...` / `python -m agents.benchmark <샘플> --answer-dir <정답키>`

---

## 0. 이 매트릭스를 읽는 규칙

| 규칙 | 내용 |
|---|---|
| **세트 병기** | 모든 수치에 어느 소스 세트 기준인지 표기한다. 섞으면 잘못된 주장이 된다 |
| **미측정 처리** | 0점 처리도, 만점 처리도 하지 않는다. **분모에서 제외**하고 미측정임을 명시 |
| **SKIPPED ≠ PASS** | 검사하지 않은 것을 통과로 집계하지 않는다 |
| **재현 가능성** | 모든 수치는 명령 한 줄로 재산출된다. LLM 판단이 섞인 수치는 없다 |

**소스 세트**

| 세트 | 구성 | `.bizunit` | 정답키 | 용도 |
|---|---|---|---|---|
| **A** `legacy-u-pla001-050` | PLA047 복제 50벌 (1유형) | 있음 | 없음 | 매핑 그래프·빌드 검증 |
| **B** `PLA081-110` | 30화면 × 5도메인 | **없음** | **있음** | 탐지 성능 채점 · 파일럿 5화면 |

---

## 1. 종합 스코어카드 — **80.9점 / 100**

| 축 | 배점 | 획득 | 달성률 |
|---|---|---|---|
| **A. 전환 성공률** | 30 | **29.40** | 98.0% |
| **B. 사용자 체감** | 25 | **19.79** | 79.2% |
| **C. 탐지 정확성** | 20 | **16.67** | 83.4% |
| **D. 기능 동등성** | 25 | **15.00** | 60.0% |
| **합계** | **100** | **80.86** | **80.9%** |

> **배점을 재조정했다(2026-09-05).** L3(기능 동등성)를 실제로 측정할 수 있게 되면서 D축을 신설하고 A 40→30 · B 30→25 · C 30→20으로 낮췄다. 근거: 이 프로젝트의 핵심 가설 H3이 "정적 검증만으로는 정확성을 보증할 수 없다"였고 AlphaTrans도 문법 96% vs 기능 25%를 보고한 만큼, **"동작이 같은가"는 "컴파일되는가"보다 무게가 커야 한다.** 탐지 정확성은 부수 가치라 가장 많이 낮췄다. 재조정으로 점수는 89.7 → **88.2로 내려갔다**(D축 커버리지가 60%라서).

> **다시 내려갔다: 88.2 → 80.9 (2026-09-05 후반).** 두 가지를 새로 재기 시작해서다.
> ① **Api(HTTP/직렬화) 계층을 처음 측정했다.** 그전까지 D축은 F 계층만 봤고, Api는 "아무도
> 검증하지 않은 층"으로 남아 있었다. 재보니 생성된 Api가 P 계층의 오케스트레이션(권한 게이트·
> 결과 메시지·레코드셋 선별)을 통째로 버리고 있었다 — 정적 검증은 전부 통과하는 코드였다.
> ② **결정론 처리 비중의 분모가 틀렸다.** P 메서드를 "규칙 기반 처리"로 세고 있었는데 사실은
> 아무 것도 변환하지 않고 있었다. 실제 포팅 대상에 넣자 75% → 46%가 됐다.
> **점수가 내려간 건 도구가 나빠져서가 아니라 측정 범위가 넓어져서다.** 안 재던 것을 재기
> 시작하면 점수는 내려간다 — 그 방향이 맞다고 보고 되돌리지 않았다.

**배점 근거**: A는 도구의 존재 이유(산출물이 안 나오면 나머지가 무의미) / B는 공수 절감의 대리 지표 / C는 정답키로 채점 가능한 부수 가치 / **D는 "맞게 옮겼는가"라는 최종 질문**.

---

## 2. 지표 매트릭스 (전체)

### A. 전환 성공률 — 40점

| ID | 지표 | 정의 | 측정 방법 | 배점 | 실측 | 점수 | 세트 |
|---|---|---|---|---|---|---|---|
| **A1** | 산출물 생성률 | 계획서가 예고한 파일이 실제로 생성된 비율 | `expected_outputs` vs 생성 파일 대조 | 12 | **100.0%** (25/25종) | **12.00** | B-5화면 |
| **A2** | 정적 검증 통과율 | BLOCKER가 하나도 없는 파일 비율 | `validate_screen()` 결과 집계 | 18 | **96.7%** (29/30건) | **17.40** | B-5화면 |
| A3 | 컴파일 통과율 | `mvn compile` 실패 파일 수 | 임시 사본 빌드 | 참고 | 실패 1건(의도된 원본 버그 보존) | — | A |
| A4 | nctRid 자동 확정률 | 메타데이터로 확정된 트랜잭션 비율 | `NCTRID_SOURCE = CONFIRMED_BIZUNIT` | 참고 | **100%** (150/150) | — | **A 전용** |

> **A4 주의**: 세트 B는 `.bizunit`이 없어 **0건**이다. "100%"는 세트 A에서만 성립한다.

### B. 사용자 체감 — 30점

| ID | 지표 | 정의 | 측정 방법 | 배점 | 실측 | 점수 | 세트 |
|---|---|---|---|---|---|---|---|
| **B1** | 리뷰 대상 축소율 | 1 − (사람이 정독할 라인 / 전체 생성 라인) | LLM 포팅 메서드 라인 + 이슈 귀속 라인 | 11 | **87.3%** | **9.60** | B-5화면 |
| **B2** | 결정론 처리 비중 | 포팅 대상 중 규칙으로 처리해 LLM을 안 부른 비율 | 계획서 `estimated_llm_calls` | 7 | **45.6%** (21/46건) | **3.20** | B-5화면 |
| **B5** | **사람 수정 수용률** | 1 − 사람 수정 라인 비율 (멘토 §H "가장 중요") | 생성 시점 스냅샷 대비 줄 단위 diff | 7 | **99.8%** (생성 1,960줄 중 4줄 수정 = 0.20%) | **6.99** | B-5화면 |
| B3 | 추적 작업 제거 | 화면당 사람이 열어야 했던 AS-IS 파일 수 | 계획서 `fragments` | 참고 | **4.0종 → 그래프 조회 1회** | — | B-5화면 |
| B4 | 낭비 호출 제거 | 재시도 루프의 무효 LLM 호출 감소 | 수정 전후 호출 수 | 참고 | **5건 → 1건 (80%↓)** | — | A |

> **B1은 스스로 깎은 지표다.** 최초에 "이슈 귀속 줄"만 세어 **99.7%**가 나왔으나, LLM이 포팅한 메서드는 이슈가 없어도 사람이 전부 읽어야 하므로 과장이다. 포팅 라인을 리뷰 대상에 포함해 71.8%로 정정했고 총점도 내려갔다(이후 규칙 처리가 늘어 LLM 포팅 라인 자체가 줄면서 **83.7%**가 됐다 — 정의를 되돌린 게 아니라 실제로 개선된 것이다).

> **B2를 올린 방식**: 배관 패턴(파라미터 전달 + 단일 조회 + 레코드셋 반환, 분기·계산 없음)을 규칙으로 추가했다. **분기·산술이 하나라도 있으면 잡지 않는다** — 비중을 올리려고 업무 로직까지 규칙으로 밀면 원칙이 뒤집힌다. 실제로 `fAuthCheck`(건수 분기)는 LLM에 남겼다.

### C. 탐지 정확성 — 30점

| ID | 지표 | 정의 | 측정 방법 | 배점 | 실측 | 점수 | 세트 |
|---|---|---|---|---|---|---|---|
| **C1** | 원본 결함 재현율 | 심어진 결함 중 탐지한 비율 | 정답키 10건 대조 | 10 | **100%** (10/10) | **10.00** | **B 전용** |
| **C2** | 중복 탐지 F1 | **EXACT 티어** 기준 재현율·정밀도 조화평균 | 정답키 330행 대조(실재 메서드만) | 10 | **66.7%** (정밀도 100% · 재현율 50%) | **6.67** | **B 전용** |
| C3 | 결함 탐지 오탐 | 정답키에 없는 파일에서의 탐지 | 파일 단위 대조 | 참고 | **0건** | — | B |
| C4 | 티어별 탐지 | EXACT(확실) / NORMALIZED(후보) | 정답키 대조 | 참고 | EXACT 6그룹·164건(«고유» 오탐 **0건**) / NORMALIZED 11그룹·161건 | — | B |
| C5 | 전체 재현율 | 두 티어 합산 | 정답키 대조 | 참고 | **209/210 (100%)** | — | B |

> **C2를 EXACT 티어로만 채점하는 이유.** NORMALIZED 티어(화면 ID 정규화)는 정답키가 «고유»라 표시한 30건 중 **29건**을 후보로 올린다. 그런데 원본을 대조해보면 `fPLA081QrySelectDetail`과 `fPLA087QrySelectDetail`은 화면 ID를 치환하면 **차이가 0**이다 — 텍스트로는 명백한 Type-2 클론이고, 정답키의 «고유»는 *업무 관점의 고유성*을 뜻한다. 어느 쪽이 맞다고 우기지 않고, **정규화를 전혀 하지 않는 EXACT 티어만 채점에 쓴다** — 이러면 티어 정의를 바꿔서 점수를 올릴 수 없다. 참고로 보수적 F1은 6%, 티어인지 F1은 100%로 극단이 갈리며, 그 사이 어디에 선을 긋느냐가 곧 «중복»의 정의를 정하는 일이다.

> **이전 68%는 틀린 값이었다.** 채점기가 정답키의 `QrySelectDetail` 항목을 `d{화면}QrySelectDetail`로 펼쳤는데 D 계층의 실제 이름은 `dPLA08102` 형태다 — **존재하지 않는 이름 30건이 분모에 섞여** 오탐률이 절반으로 희석돼 있었다. 실재하는 메서드만 세도록 고쳤다.

### D. 기능 동등성 — 25점

| ID | 지표 | 정의 | 측정 방법 | 배점 | 실측 | 점수 | 세트 |
|---|---|---|---|---|---|---|---|
| **D1** | **F 계층 실행 일치율** | 같은 입력·같은 D 계층 데이터에서 AS-IS F와 TO-BE Service가 같은 값을 내는 비율 | 최소 실행 하네스(`agents/equivalence_test.py`), 0행/1행/3행 | 9 | **100%** (48/48) | **9.00** | B-5화면 |
| **D3** | **Api 계층 실행 일치율** | AS-IS P 메서드와 TO-BE Api 메서드가 같은 입력에 같은 응답을 내는 비율 | 같은 하네스, 응답 전체 비교 | 6 | **0%** (0/9) — 페이로드만 보면 **100%** (9/9) | **0.00** | B-5화면 |
| **D2** | 동등성 검증 커버리지 | 실행 비교가 가능했던 화면 비율 | 컴파일 성공 화면 / 대상 화면 | 10 | **60%** (3/5) | **6.00** | B-5화면 |

> **D1을 둘로 나눈 이유(2026-09-05).** F 계층만 재면 "업무 로직은 옮겼는데 진입점은 안 옮겼다"가
> 100%로 보인다. 실제로 그 상태였다 — 생성된 Api가 `service.fAuthCheck(request)` 한 줄이라 원본의
> 권한 게이트와 결과 메시지가 사라져 있었고, F 계층 100% 뒤에 완전히 가려져 있었다. 층을 나눠
> 세우자마자 Api 0%가 드러났다.
>
> **Api 0%가 무엇을 뜻하는지는 나눠 읽어야 한다.** 페이로드(반환하는 레코드셋)는 고친 뒤
> **9/9 일치**한다. 남은 차이는 전부 결과 메시지 코드(`@msg=I0016` 등)이고, 이걸 TO-BE 응답에
> 어떤 키로 실을지는 **아직 확정되지 않은 공통 규약**이다(docs/09 열린 질문 2번 — 이번에 실사용
> 근거를 찾아 "W/I는 예외가 아니라 성공 응답의 안내 메시지"까지는 확정했지만, 담을 자리를 정하는
> 건 사람 몫이다). 규약 없이 임의의 키를 만들지 않기로 해서 생성기가 그 자리를 비워뒀고, 그래서
> 엄격 일치는 0%다. **점수는 엄격 일치로 매긴다** — 우리가 못 정했다는 이유로 후하게 주지 않는다.
>
> **Api 계층은 1화면(9케이스)만 측정됐다.** 나머지 4화면은 AS-IS P/F 원본에 주입 결함이 있어
> 컴파일되지 않는다. 배점 6점이 표본 1화면에 걸려 있다는 뜻이므로, 이 항목은 다른 항목보다
> 신뢰구간이 넓다.

> **일치율과 커버리지를 따로 잡는 이유.** 실행된 케이스만으로 100%가 나와도 실행하지 못한 화면이 있으면 "전부 검증됐다"가 아니다. 한 수치로 합치면 부분 검증이 전체 검증처럼 보인다 — 리뷰 축소율에서 이미 한 번 겪은 실수라 같은 형태를 반복하지 않는다. 실행하지 못한 2화면은 **AS-IS 원본이 컴파일되지 않아서**(주입 결함)이지 도구의 실패가 아니다.

### E. 아직 측정하지 못한 것 (분모에서 제외)

| ID | 지표 | 왜 못 쟀나 | 해소 조건 |
|---|---|---|---|
| **D1** | **기능 동등성** (응답 diff 일치율) | 포팅된 Service를 **실행할 환경이 없다** | 애플리케이션 기동 또는 테스트 동반 생성 |
| ~~D2~~ | ~~사람 수정 라인 비율~~ | **측정 완료 → B5로 이동** (4.00%). 단 **AI 리뷰어 1차 리뷰 기준**이므로 하한값이다 | 사람 개발자 리뷰로 재측정 |
| D3 | 화면당 공수 절감률 | 기준선(수작업 공수) 자체가 미실측 | 파일럿 10~20건 실투입 시간 측정 |

> **이 점수는 L1(구조)·L2(문법) 층과 그 대리 지표에 대한 것이지, "전환이 기능적으로 맞다"는 뜻이 아니다.**

---

## 3. 검증 층위 매트릭스 (L1~L4)

| 층 | 무엇을 묻나 | 수단 | 상태 | 관련 지표 |
|---|---|---|---|---|
| **L1 구조** | 만들 것이 다 만들어졌나 | 계획서 vs 산출물 대조 | **통과** | A1, A4 |
| **L2 문법** | 컴파일되나, 참조가 실재하나 | 정적 검증 + `mvn compile` | **통과** | A2, A3, C1~C4 |
| **L3 기능** | 같은 입력에 같은 값을 내나 | 차등 테스트 | **부분** — SQL 계층 1건만 | D1 |
| **L4 공수** | 사람이 얼마나 고쳤나 | 사람 수정 라인 비율 | **부분** — 4.00%(AI 리뷰어 기준) | B5, D3 (대리: B1, B2) |

**각 층의 통과를 다음 층의 통과로 오인하지 않는다.** 가설 H3("정적 검증만으로 정확성을 보증할 수 있다")이 반증된 근거가 이 구분이다.

---

## 4. 판정 체계 (도구·DB·문서 공통 용어)

| 판정 | 의미 | 저장 가능 | 집계 규칙 |
|---|---|---|---|
| **PASS** | 통과 | 가능 | 분자에 포함 |
| **WARNING** | 동작하나 확인 필요 | 가능(승인자 판단) | 통과로 집계 |
| **BLOCKER** | 정상 동작 보장 불가 | **불가** | 실패로 집계 |
| **FAIL** | 검사 자체가 실패 | **불가** | 실패로 집계 |
| **SKIPPED** | 도구 미지원 | 가능(미검증 표기) | **PASS로 집계 금지** |
| **APPROVED** | 사람이 검토·승인 | — | 자동 부여 금지 |

`ORIGINAL_BUG` 표시로 **원본 결함과 도구의 실패를 구분**한다.

---

## 5. 개선 이력 (before → after)

| 항목 | Before | After | 계기 |
|---|---|---|---|
| 원본 결함 탐지 | 5/10 (50%) | **10/10 (100%)** | 놓친 유형별로 탐지기 5종 추가 |
| 중복 탐지 재현율 | 105/210 (50%) | **209/210 (100%)** | 화면 ID 정규화(Type-2) |
| 중복 탐지 신뢰 수준 | 단일 목록(오탐 섞임) | **EXACT/NORMALIZED 2티어** | 확실한 것과 검토 후보 분리 |
| 중복 채점 분모 | 유령 이름 30건 포함 *(버그)* | **실재 메서드만** | 채점기 버그 수정 |
| 리뷰 대상 축소율 | 99.7% *(과장)* | **71.8%** | LLM 포팅 라인을 리뷰 대상에 포함 |
| 수리 루프 | 2라운드 소진 후 **악화·포기** | **1라운드 해결** | TO-BE 파서 신설(코드가 프롬프트에 실림) |
| LLM 낭비 호출 | 화면당 5건 | **1건 (80%↓)** | 포팅 대상을 `LLM_PENDING`로 한정 |
| 결정론 처리 비중 | 46.4% | **75.0%** | 배관 패턴 규칙 추가 |
| 리뷰 대상 축소율 | 71.8% | **83.7%** | 규칙 처리 증가로 LLM 포팅 라인 감소 |
| 사람 수정 라인 측정 | **수단 없음** | **구현 완료** (값은 리뷰 후) | 스냅샷 기반 diff |
| 사람 수정 라인 | 미측정 | **4.00% 실측** | 스냅샷 대비 diff (AI 리뷰어 1차) |
| 중복 탐지 티어 | 2티어 | **3티어 + 저정보 판정** | AST 실험 → 음성 결과를 규칙으로 |
| L3 기능 동등성(F 계층) | **측정 불가**(전제가 틀렸음) | **48/48 일치 · 화면 3/5** | 최소 실행 하네스 신설 |
| L3 기능 동등성(Api 계층) | **미측정** | **페이로드 9/9 · 메시지 0/9** | P 오케스트레이션 포팅 신설 |
| 측정 대상 메서드 | 화면당 3개(5개 중) | **화면당 5개 전수** | 개명된 메서드 짝짓기 수정 |
| 공통 메서드 중복 | 화면마다 30벌 복제 | **Store 3종·SQL 3종을 1벌로** | 공통화 분석 + 확정 레지스트리 |
| 배관 규칙 의미 정확성 | 33% 일치 | **100%** | 하네스가 레코드셋 추출 누락을 잡아냄 |
| 종합 점수 | 75.7 → 89.7 → 88.2 | **80.9** (Api 계층 측정 후) | 위 누적 |

---

## 6. 신뢰성 근거 — 이 수치를 믿어도 되는 이유

| 근거 | 내용 |
|---|---|
| **정답키 대조** | 결함 10건·중복 330행의 정답이 외부에서 주어졌다. 자체 채점이 아니다 |
| **교차 검증** | 탐지기 만점이 과적합인지 확인하려 **다른 세트**에 돌렸고, 거기서 프로젝트가 여태 못 본 실제 결함을 찾았다(파라미터명 누락, 계층 조회 오용) |
| **버린 후보 기록** | 오탐 54건 난 탐지기와 전 파일에 뜨는 신호는 **폐기했고 그 사실을 남겼다** |
| **결정론성** | 모든 지표가 LLM 없이 산출된다 — 재실행하면 같은 값 |
| **불리한 정정** | 유리한 정의(99.7%)를 버리고 방어 가능한 정의를 택해 총점이 내려갔다. 중복 채점도 부풀려져 있던 68%를 버그 수정 후 66.7%로 낮췄다 |
| **전제 검사** | 규칙이 만들 코드의 전제(호출 대상 실재)가 깨지면 규칙이 **스스로 사양**하고 LLM 경로로 넘긴다 — 비중을 올리려 잘못된 코드를 찍어내지 않는다 |

---

## 7. 한계 (이 매트릭스가 말하지 않는 것)

1. **기능 동등성은 부분 측정이다.** F 계층 일치율은 48/48(100%)이지만 **화면 3/5만 실행 가능**했고(나머지는 AS-IS 원본이 컴파일 불가), 캔드 데이터는 합성값이라 운영 데이터 분포를 대표하지 않는다. **Api 계층은 1화면(9케이스)만 측정됐다** — 배점 6점이 표본 1건에 걸려 있다. 서블릿/Jackson을 거치는 진짜 HTTP 왕복은 여전히 미검증이다(메서드 호출 수준까지만 비교했다).
2. **사람 수정 0.20%는 리뷰가 찾은 결함 수와 다르다.** 이번 리뷰가 찾은 결함 3건 중 2건(엔드포인트 경로 충돌, 단순 위임의 레코드셋 누수)은 산출물이 아니라 **생성기를 고쳐** 해소해서 수정 라인으로 잡히지 않는다. 게다가 AI 리뷰어 1차 기준이라 사람 개발자 리뷰로는 더 올라간다 — 하한값의 하한값으로 읽어야 한다.
2. **파일럿이 5화면이다.** 통계적 신뢰구간을 말할 규모가 아니다.
3. **배점 가중치(40/30/30)는 설계 판단이다.** 근거는 밝혔으나 유일한 정답은 아니다.
4. **세트 B는 합성 데이터다.** 실제 운영 소스의 결함 분포와 다를 수 있다.
5. **중복 탐지 EXACT 재현율이 50%다.** 정규화 없이 잡을 수 있는 한계이며, 이를 올리려면 정규화가 아닌 다른 방법(구조 기반 비교 등)이 필요하다.
